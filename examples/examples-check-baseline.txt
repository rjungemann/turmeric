# Examples that fail `tur check` today. tests/check-examples.sh treats these as
# expected failures, and fails if one starts passing (drop it from here) or if
# any example NOT listed starts failing.
#
# Added 2026-08-20 while resolving docs/reported/datalog-examples-do-not-compile.
# Each needs its own fix; the open residue is tracked in
# docs/reported/examples-tree-does-not-run.md.
#
# The five datalog files and cli_args_demo left this list on 2026-08-21. Of
# what remains, only ONE entry is example code being wrong -- the guestbook
# seven are
# arguably the wrong invocation (they are a multi-file program with no
# import/load between the files, compiled as a unit by their CMakeLists, so
# `tur check <one member>` was never going to work).  snake left this list on
# 2026-09-02 when perform-inside-loop-has-no-lowering was resolved.

