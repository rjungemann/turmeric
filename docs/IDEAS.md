* λ as an alias for `fn`, ∀ as an alias for `forall`, ∃ as an alias for `exists`
* `(as-> expr name & forms)` https://clojuredocs.org/clojure.core/as-%3E
* `(. instance function arg1 ...)` as a synonym for `(.function instance arg1 ...)`
* Clojure-style function shorthand with `#()` and so on