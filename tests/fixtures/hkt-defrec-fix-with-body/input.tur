;; Phase HKT-P2: defrec with body
;; Fix is the fixed-point type constructor: Fix[f] ≅ f(Fix[f])
;; In v1, the body is accepted but not fully evaluated

(defrec Fix [^f] (Fix (^f (Fix ^f))))

(defn main [] :int 0)
