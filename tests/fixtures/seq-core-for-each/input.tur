; LZ0 fixture: seq-for-each -- call a function for every element.

(load "stdlib/seq/core.tur")

(defn main [] :void
  (seq-for-each println (seq-of 5))
  (seq-for-each println (seq-of 6))
  (seq-for-each println (seq-of 7)))
