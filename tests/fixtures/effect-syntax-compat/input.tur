;; Phase 19 compatibility: historical defeffect return type syntax (symbol form).

(defeffect Read [] int)

(defn compat-smoke [] :int
  (do
    (handle 7
      (Read [] k) 0)
    7))

(println (compat-smoke))
