; LZ3 fixture: seq/concat -- yield all elements of s1 then all of s2.
; Concatenates [0,3) and [10,12): expected 0 1 2 10 11

(load "stdlib/seq/combine.tur")

(defn main [] : void
  (seq-for-each println (seq/concat (seq/range 0 3) (seq/range 10 12))))
