;; U6: inline-C block inside #{Unsafe} function should not warn
(defn raw-add [a : int b : int] #fx{Unsafe} : int
  ```c
  return a + b;
  ```)

(defn main [] : int
  (unsafe (println (raw-add 1 2)))
  0)
