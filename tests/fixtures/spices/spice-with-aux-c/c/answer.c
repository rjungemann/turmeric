/* Hand-written vendored C source, compiled by the spice build because it is
 * listed in :c-sources. Finds its own header through :c-includes ["c/inc"]. */
#include "answer.h"

int aux_answer(void) {
    return 42;
}
