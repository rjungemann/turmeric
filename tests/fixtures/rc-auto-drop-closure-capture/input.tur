;; Phase 9 auto-drop: closure capture should not suppress defer injection for rc binding.

(defn rc-auto-drop-closure-capture []
  (let [x (rc/of 7)]
    (let [f (fn [] (rc/strong-count x))]
      (f))))

(defn main []
  (println (rc-auto-drop-closure-capture))
  0)
