;; Phase 9 auok?-drop: closure capture should not suppress defer injection for rc binding.

(defn rc-auok?-drop-closure-capture []
  (let [x (rc/of 7)]
    (let [f (fn [] (rc/strong-count x))]
      (f))))

(defn main []
  (println (rc-auok?-drop-closure-capture))
  0)
