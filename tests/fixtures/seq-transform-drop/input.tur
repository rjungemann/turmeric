; LZ2 fixture: seq/drop -- skip the first n elements.

(load "stdlib/seq/transform.tur")

(defn main [] : void
  (seq-for-each println (seq/drop 2 (seq/range 0 5)))
  (println (gen-arr-len (seq-collect (seq/drop 5 (seq/range 0 3))))))
