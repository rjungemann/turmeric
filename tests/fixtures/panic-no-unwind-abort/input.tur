;; Phase R5: #[no-unwind] function that actually panics must call abort().
;; The process exits non-zero and stderr contains "panic (no unwind):".
(defn #[no-unwind] abort-op [x] :int
  (do
    (panic "no-unwind triggered")
    x))

(defn main [] :int
  (abort-op 0))
