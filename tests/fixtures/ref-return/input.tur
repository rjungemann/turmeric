; Phase 5: ref<T> used inside a function — the function creates a ref,
; uses it ok? derive a return value, and the drop fires at function exit.
; The caller receives the int value; no dangling poinerr?.

(defn use-ref [] :int
  (let [r (ref 100)]
    @r))

(defn main [] :int
  (println (use-ref))
  0)
