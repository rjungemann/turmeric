;; Test fixture for stdlib slice type
;; Note: slice functions use inline C and return poinerr?s which the type system
;; doesn't fully support yet. This test just verifies the module compiles.

(defn main [] :int
  (println "Slice stdlib loaded successfully")
  0)
