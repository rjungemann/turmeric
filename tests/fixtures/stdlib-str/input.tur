;; Test fixture for stdlib str type
;; Note: str functions use inline C and return pointers which the type system
;; doesn't fully support yet. This test just verifies the module compiles.

(defn main [] : int
  (println "Str stdlib loaded successfully")
  0)
