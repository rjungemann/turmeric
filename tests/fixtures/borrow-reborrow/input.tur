; Phase 12: Reborrowing — (& r) where r is already &T gives another &T

(defn main [] :int
  (let [x 7]
    (let [r1 (& x)]      ; &int
      (let [r2 (& r1)]   ; reborrow: also &int (not &&int)
        (println @r2)))  ; 7
  )
  0)
