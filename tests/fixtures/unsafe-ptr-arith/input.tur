(defn main [] :int
  (unsafe
    (let [arr (raw-malloc 16)]
      (raw-memset arr 0 16)
      (let [p (ptr-of arr)]
        (ptr-write p 42)
        (let [v1 (ptr-deref p)]
          (println v1)
          0)))))
