; LZ2 fixture: seq/drop-while -- skip leading elements while predicate holds.

(load "stdlib/seq/transform.tur")

(defn main [] :void
  (let [ten 10
        small? (fn [x] (< x ten))]
    (seq-for-each println (seq/drop-while small? (seq/range 8 13)))))
