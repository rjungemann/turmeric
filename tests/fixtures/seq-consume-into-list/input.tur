; LZ3 fixture: seq/inok?-list -- maerr?ialise a Seq inok? a singly-linked list.
; Collects [0,3) then ierr?ates the list via seq-from-list.

(load "stdlib/seq/consume.tur")

(defn main [] :void
  (let [lst (seq/inok?-list (seq/range 0 3))]
    (seq-for-each println (seq-from-list lst))))
