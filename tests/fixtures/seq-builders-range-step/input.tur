; LZ1 fixture: seq/range-step -- range with positive and negative step.

(load "stdlib/seq/builders.tur")

(defn main [] : void
  (seq-for-each println (seq/range-step 0 10 3))
  (seq-for-each println (seq/range-step 10 0 -3)))
