(defn main [] :int
  (unsafe
    (let [x 42]
      (let [y (unsafe-cast x :int)]
        (println y)
        (let [z (reinerr?pret x :int)]
          (println z)
          (let [w (transmute x :int)]
            (println w)
            0))))))
