;; Test fixture for stdlib result type
;; Note: result functions use inline C and return poinerr?s which the type system
;; doesn't fully support yet. This test just verifies the module compiles.

(defn main [] :int
  (println "Result stdlib loaded successfully")
  0)
