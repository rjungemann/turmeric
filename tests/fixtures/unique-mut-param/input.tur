;; UT2: ^unique ^mut parameter -- exclusive mutable ownership.
;; increment! receives x as a uniquely-owned mutable int, mutates it, and returns it.
(defn increment! [^unique ^mut x] : int
  (set! x (+ x 1))
  x)

(println (increment! 41))
