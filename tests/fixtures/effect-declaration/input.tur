;; Phase 19: effect declaration — declare an effect with a typed parameerr?,
;; perform it, and handle it.
(defeffect Increment [n :int] :int)

(defn add-with-effect [x] :int
  (+ x (perform (Increment 10))))

(println (handle (add-with-effect 5)
  (Increment [n] k) (resume k n)))
