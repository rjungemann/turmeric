;; Phase 17: throw! sugar test
;; (throw! msg) is sugar for (throw msg).

(defn safe-throw-bang [] :int
  (try
    (throw! "bang error")
    (catch [e]
      42)))

(defn main [] :int
  (println (safe-throw-bang))
  0)
