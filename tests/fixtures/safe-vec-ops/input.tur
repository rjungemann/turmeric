(defn main [] :int
  (unsafe
    (let [arr (raw-malloc 16)]
      (raw-memset arr 0 16)
      (let [p (ptr-of arr)]
        (ptr-write p 42)
        (let [result (array-set p 0 99)]
          (println result)
          0)))))
