; LZ1 fixture: seq/range -- half-open integer range [start, end).

(load "stdlib/seq/builders.tur")

(defn main [] :void
  (seq-for-each println (seq/range 0 5))
  (println (gen-arr-len (seq-collect (seq/range 3 3)))))
