(defeffect Inner [] :int)
(defeffect Outer [] :int)
(defn body [] : int (+ (perform (Inner)) (perform (Outer))))
(defn run [] : int
  (handle
    (handle (body) (Inner [] k) (resume k 10))
    (Outer [] k) (resume k 20)))
(defn main [] : int (println (run)) 0)
