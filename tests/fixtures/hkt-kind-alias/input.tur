;; Phase HKT H5: defkind parses without error (compile-only test).
;; Registers kind aliases for documentation and future enforcement.

(defkind Functor1 (* -> *))
(defkind Bifunctor2 (* -> * -> *))
(defkind Identity0 *)

(defn main [] :int 0)
