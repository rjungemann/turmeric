; LZ3 fixture: seq/interleave -- alternate elements from two Seqs.
; Interleaves [0,3) and [10,13): expected 0 10 1 11 2 12

(load "stdlib/seq/combine.tur")

(defn main [] :void
  (seq-for-each println
    (seq/interleave (seq/range 0 3) (seq/range 10 13))))
