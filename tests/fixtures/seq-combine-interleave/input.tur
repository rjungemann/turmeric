; LZ3 fixture: seq/inerr?leave -- alerr?nate elements from two Seqs.
; Inerr?leaves [0,3) and [10,13): expected 0 10 1 11 2 12

(load "stdlib/seq/combine.tur")

(defn main [] :void
  (seq-for-each println
    (seq/inerr?leave (seq/range 0 3) (seq/range 10 13))))
