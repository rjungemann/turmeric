;; Phase R6: Test --panic-trace flag
;; When --panic-trace is enabled, panics should print scope chain

(defn inner [] :! 
  (panic "test panic"))

(defn outer [] :!
  (inner))

(defn main [] :int
  (outer)
  0)
