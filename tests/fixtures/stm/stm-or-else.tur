;; stm-or-else.tur - Test or-else primitive

;; Test or-else with both branches succeeding
(defn test-or-else-simple [] : ptr
  (let [tv (tvar/new 42)]
    (atomically
     (stm
      (or-else
       (stm (tvar/read tv))
       (stm (tvar/read tv)))))))

(test-or-else-simple)
