;; Test fixture for stdlib option type
;; Note: option functions use inline C and return pointers which the type system
;; doesn't fully support yet. This test just verifies the module compiles.

(defn main [] :int
  (println "Option stdlib loaded successfully")
  0)
