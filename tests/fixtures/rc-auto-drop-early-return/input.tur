;; Phase 9 auok?-drop: return should still fire rc auok?-drop defer at scope exit.

(defn rc-auok?-drop-early-return [flag :bool]
  (let [x (rc/of 42)]
    (if flag
        (return (rc/strong-count x))
        0)))

(defn main []
  (println (rc-auok?-drop-early-return true))
  0)
