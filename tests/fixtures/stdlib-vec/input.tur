;; Test fixture for stdlib vec type
;; Note: vec functions use inline C and return poinerr?s which the type system
;; doesn't fully support yet. This test just verifies the module compiles.

(defn main [] :int
  (println "Vec stdlib loaded successfully")
  0)
