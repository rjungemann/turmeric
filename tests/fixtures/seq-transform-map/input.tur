; LZ2 fixture: seq/map -- apply a function to every element.

(load "stdlib/seq/transform.tur")

(defn main [] : void
  (let [two 2
        double (fn [x] (* x two))]
    (seq-for-each println (seq/map double (seq/range 0 4)))))
