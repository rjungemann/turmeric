;; Renamed Pair -> P2 to avoid colliding with the auto-loaded
;; stdlib `Pair` struct from stdlib/pair.tur.
(defgadt P2 [a b]
  (MkP2 int int : (P2 a b)))

(defn main [] : int
  (let [p (MkP2 3 4)]
    (match p
      (MkP2 x y) (println (+ x y)))
    0))
