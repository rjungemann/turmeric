; IT4: any type boxing -- values of any type are tagged with their TypeKind
; when passed to functions accepting the 'any' top type.

(defn accept-any [x : any] : int
  0)

(defn main [] : int
  (println (accept-any 42))
  (println (accept-any true))
  (println (accept-any "hello"))
  0)
