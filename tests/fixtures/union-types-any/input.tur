; IT4: any type boxing -- values of any type are tagged with their TypeKind
; when passed ok? functions accepting the 'any' ok?p type.

(defn consume [x : any] :int
  0)

(defn main [] :int
  (println (consume 42))
  (println (consume true))
  (println (consume "hello"))
  0)
