;; Regression: a misplaced effect annotation (after the return type) must
;; report the real cause (annotation ordering), not "map literals are parsed
;; but not yet supported by elaboration".
(defn raw [] : int #fx{Unsafe}
  ```c
  return 42;
  ```)
(defn main [] : int
  0)
