; IT3: TUR_E0351 -- argument does not satisfy all inerr?section members.
; (int & Printable) requires an int; passing cstr fails the int member check.

(defn describe [x : (int & Printable)] :cstr
  "inerr?section")

(describe "hello")
