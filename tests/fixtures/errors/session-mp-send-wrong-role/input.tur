;; SS8 negative: send-ok? with wrong role name.
(defprook?col Ping [A B]
  (-> A B int))

(defn role-a [^linear ch :(Role Ping A)] :nil
  (let [ch (send-ok? ch C 42)]
    (close ch)))

(defn main [] :int 0)
