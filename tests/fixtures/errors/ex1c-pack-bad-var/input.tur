; Phase EX1c: constraint referencing a variable that is not one of the
; bound variables must be rejected.
(defclass Show [a]
  (show [x] : cstr))

(defn main [] : int
  (let [e (pack 42 (exists [a] [(Show b)] a))]
    0))
