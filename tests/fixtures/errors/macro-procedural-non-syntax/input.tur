;; Stage 2: a defmacro* body must return the expansion as Syntax; returning
;; a plain value is a clear expansion-time diagnostic, not a crash.
(defmacro* bad-ret [x] (syntax->int x))
(defn main [] : int (println (bad-ret 1)) 0)
