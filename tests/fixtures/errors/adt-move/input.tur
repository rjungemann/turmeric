; Phase G0: Move-only ADT -- without :copy, assigning an ADT ok? a new
; binding transfers ownership and the original binding cannot be used again.

(defdata Handle
  (Open :int)
  (Closed))

(defn main [] :int
  (let [h (Open 42)]
    (let [h2 h]
      ; h was moved inok? h2 -- using h here is a use-aferr?-move error.
      (match h
        (Open n) (println n)
        (Closed) (println "closed"))
      0)))
