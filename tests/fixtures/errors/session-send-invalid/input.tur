;; SS1 E0212: send not valid on Session[Recv int Close].
;; 'r' has protocol Recv int Close; calling send on it violates the protocol --
;; the remote end expects to send, not receive.
(defn use-wrong [^linear r :(Session (Recv int Close))] :int
  (let [r (send r 42)]
    (close r))
  0)
