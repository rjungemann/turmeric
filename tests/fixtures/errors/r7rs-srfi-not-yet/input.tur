#lang r7rs
;; r7rs-srfi-plan S1: a planned SRFI whose stage has not landed is refused,
;; naming it and the table that says which SRFIs are here.  SRFI 1 lands at
;; S3; when it does, pick another not-yet row for this pin.
(import (scheme base) (srfi 1))
