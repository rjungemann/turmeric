;; FH6: a ^linear continuation in a handler value must be resumed exactly once.
(defeffect Choose [] :int)
(defn main [] : int
  (with-handler
    (handler (Choose [] ^linear k) (do (resume k 1) (resume k 2)))
    (perform (Choose))))
