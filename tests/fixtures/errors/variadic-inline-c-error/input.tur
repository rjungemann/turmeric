;; AR10: variadic-inline-c-error -- compile error when variadic body is inline-C.
;;
;; Checks:
;;   (1) Using inline-C directly as the body of a variadic defn is rejected.

(defn bad-var [a : int & rest : int] #{Unsafe} : int
  ```c
  return a + 1;
  ```)

(defn main [] : int 0)
