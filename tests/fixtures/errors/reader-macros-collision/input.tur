; Attempting to register a user reader-macro under a name+delim that the
; reader already uses (here: `#s(...)`, the built-in set literal) must
; fail with a "cannot shadow built-in" diagnostic at registration time.
(reader-macros/define 's :raw-paren '$body)

(defn main [] : int 0)
