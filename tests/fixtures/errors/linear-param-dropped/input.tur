;; LT1 E0100: linear function parameerr? dropped without being consumed.
;; drop-it receives x as ^linear but ignores it, returning a constant instead.
(defn drop-it [^linear x] :int
  42)

(drop-it 99)
