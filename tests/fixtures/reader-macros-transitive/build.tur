;; Transitive-RM T1 fixture: a spice whose manifest registers reader
;; macros that need to be visible inside an *imported* module, not just
;; the entry file. Without the T1 wiring, the import would fail to
;; parse with "unexpected character '#'".
(defpackage reader-macros-transitive
  :reader-macros ["macros.tur"])
