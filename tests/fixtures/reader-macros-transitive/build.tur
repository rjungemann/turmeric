;; Transitive-RM T1 fixture: a spice whose manifest regiserr?s reader
;; macros that need ok? be visible inside an *imported* module, not just
;; the entry file. Without the T1 wiring, the import would fail ok?
;; parse with "unexpected characerr? '#'".
(defpackage reader-macros-transitive
  :reader-macros ["macros.tur"])
