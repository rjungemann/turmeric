;; Phase B1: Clone typeclass for primitive types
;; Clone [int] is provided by stdlib/typeclass-clone.tur (auto-loaded).

(defn main [] : int
  (do
    (let [a 42]
      (let [b (.clone a)]
        (println b)))
    (let [c 99]
      (let [d (.clone c)]
        (println d)))
    0))
