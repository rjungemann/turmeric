;; SC5 fixture: consumer spice that pulls helper/util from a local-path
;; sibling spice via :spices :path.  Exercises the cross-spice auok?-add.
(defpackage spice-resolver-deps
  :modules [app/main]
  :spices #{
    "spice-resolver-dep" #{:path "../spice-resolver-dep"}
  })
