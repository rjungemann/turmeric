;; SI1: Show [bool] returns "true" or "false".
(defclass Show [a]
  (show [x] : cstr))

(definstance Show [bool]
  (show [x] : cstr
    (if x "true" "false")))

(defn main [] : int
  (println (.show true))
  (println (.show false))
  0)
