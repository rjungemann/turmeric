;; LT3: lref/new produces an lref<T> (linear owning pointer).
;; The returned value is consumed by @ (deref), satisfying linearity.
(defn sink [v] :int v)
(defn use-lref [] :int
  (let [p (lref/new 99)]
    (sink (deref p))))
(println (use-lref))
