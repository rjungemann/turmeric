;; Phase R5: Test that panics don't cross FFI boundaries
;; This fixture documents the rule: panics must not cross extern-c boundaries
;; without catch-unwind or #[no-unwind].

(defn main [] :int
  0)
