;; Annotations before the name are consumed in any order, matching `let`.
(def ^mut ^persistent a 1)
(def ^persistent ^mut b 2)

(defn main [] : int
  (set! a 10)
  (set! b 20)
  (println (+ a b))
  0)
