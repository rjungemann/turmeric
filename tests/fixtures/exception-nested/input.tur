;; Phase 17: Nested try/catch test

(defn test-nested-try []
  (try
    (try
      (throw 42)
      (catch [e]
        (throw 43)))
    (catch [e]
      0)))

(defn main []
  (println "exception-nested test")
  0)
