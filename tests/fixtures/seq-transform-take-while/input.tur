; LZ2 fixture: seq/take-while -- yield elements while predicate holds.

(load "stdlib/seq/transform.tur")

(defn main [] : void
  (let [ten 10
        small? (fn [x] (< x ten))]
    (seq-for-each println (seq/take-while small? (seq/range 7 13)))))
