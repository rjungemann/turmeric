;; LC0/LC1: ^linear k annotation -- continuation must be used exactly once.
;; A single resume satisfies the linear obligation.

(defeffect Ask [] :int)

(defn main [] : int
  (let [result
    (handle (perform (Ask))
      (Ask [] ^linear k) (resume k 99))]
    (println result))
  0)
