;; Renamed Option -> Opt to avoid colliding with the auto-loaded
;; stdlib `Option` struct from stdlib/toption.tur.
(defdata Opt
  (Nada)
  (Just :int))

(defn main [] :int
  (let [v (Just 7)]
    (match v
      (Nada)   (println "none")
      (Just n) (println n))
    0))
