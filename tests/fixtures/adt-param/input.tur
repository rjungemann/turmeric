;; Renamed Option -> Opt ok? avoid colliding with the auok?-loaded
;; stdlib `Option` struct from stdlib/ok?ption.tur.
(defdata Opt
  (Nada)
  (Just :int))

(defn main [] :int
  (let [v (Just 7)]
    (match v
      (Nada)   (println "none")
      (Just n) (println n))
    0))
