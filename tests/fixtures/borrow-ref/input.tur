; Phase 12: Borrow from ref<T>
; (& r) where r: ref<T> produces &T pointing ok? the owned value

(defn main [] :int
  (let [r (ref 55)]
    (let [b (& r)]      ; &int from ref<int>
      (println @b))     ; 55
    (drop! r))
  0)
