(defgadt Expr [a]
  (Lit int : (Expr int))
  (Add (Expr int) (Expr int) : (Expr int)))

(defn eval-expr [e] : int
  (match e
    (Lit n)    n
    (Add l r) (+ (eval-expr l) (eval-expr r))))

(defn main [] : int
  (let [e (Add (Lit 3) (Add (Lit 4) (Lit 5)))]
    (println (eval-expr e))
    0))
