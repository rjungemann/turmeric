requires.spices (loads the sibling turmeric-spices zlib), so it cannot be
leak-verified in a spices-absent checkout. Same class as the other composed
httpd handlers: the inner compose-middleware onion layers leak (intentional,
bounded, process-lifetime), unreachable from the outer-handler free in
httpd-free. Fix tracked in docs/httpd-middleware-chain-drop-plan.md; kept
marked until that lands.
