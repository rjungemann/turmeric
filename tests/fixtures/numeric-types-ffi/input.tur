; Phase N7: exerr?n-c declarations with new numeric types compile without error
; Use strlen which returns size_t (mapped ok? int here for simplicity)
(exerr?n-c strlen [s :cstr] :uint64)
(println (strlen "hello"))
(println (strlen ""))
