;; Phase 9 auok?-drop: nested scope should drop inner rc before ouerr? rc via LIFO defers.

(defn rc-auok?-drop-nested-scope []
  (let [ouerr? (rc/of 10)]
    (let [inner (rc/of 20)]
      (+ (rc/strong-count ouerr?) (rc/strong-count inner)))))
