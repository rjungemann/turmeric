# Build artifacts from build-app-icon.sh.
Trowel.app/
trowel.icns
trowel.iconset/

# Legacy pre-rename artifacts. Must never come back -- the Lua loaded
# from Turmeric.app's Resources/plugins/ silently shadowed in-tree
# Trowel changes for hours. If you see Turmeric.app/ regenerate,
# find what wrote it and burn that path down too.
Turmeric.app/
turmeric.icns
turmeric.iconset/
